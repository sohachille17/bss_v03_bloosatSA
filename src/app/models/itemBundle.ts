export interface ItemBundle {

  name?: string,
  price?: string,
  quantity?: string,
  total?: string,
  siteId?: string,
  type?: string,
  productId?: string,

  // interface for bundleItems   -- > Product Site (BLOOSAT achillesoh15@gmail.com)
}
